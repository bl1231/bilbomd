---
'@bilbomd/mongodb-schema': minor
'@bilbomd/bilbomd-types': minor
'@bilbomd/backend': minor
'@bilbomd/ui': minor
---

Implement structured Data Transfer Object (DTO) to decouple mongodb entries from frontend logic.
Added a new package for shared types `bilbomd-types`.
Added `results` to MongoDB Job schema.
Extensive refactoring of `ui` React components.