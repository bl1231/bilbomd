{
  "name": "@bilbomd/worker",
  "version": "2.17.4",
  "description": "Worker app to process BilboMD jobs from BullMQ queue",
  "main": "dist/worker.js",
  "files": [
    "dist",
    "public",
    "views",
    "scripts"
  ],
  "type": "module",
  "scripts": {
    "clean": "rimraf dist/*",
    "copy-assets": "copyfiles --all --up 1 --exclude \"./**/*.{ts,tsx}\" \"./src/**/*\" ./dist",
    "lint": "eslint",
    "tsc": "tsc",
    "tsc:build": "tsc -p tsconfig.build.json",
    "build": "pnpm run clean && pnpm run copy-assets && pnpm run tsc:build",
    "build:ci": "pnpm run clean && pnpm run copy-assets && pnpm run lint && pnpm run tsc:build",
    "dev:start": "pnpm run build && pnpm run start",
    "dev": "nodemon --watch src --ext ts,ejs --exec npm run start",
    "start": "node dist/worker.js",
    "test": "vitest --run --coverage",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:python": "python -m pytest scripts/ -v"
  },
  "keywords": [],
  "author": "Scott Classen",
  "license": "ISC",
  "dependencies": {
    "@bilbomd/bilbomd-types": "workspace:*",
    "@bilbomd/md-utils": "workspace:*",
    "@bilbomd/mongodb-schema": "workspace:*",
    "axios": "^1.20.0",
    "axios-retry": "^4.5.0",
    "bullmq": "^6.3.4",
    "csv-parser": "^3.2.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "fs-extra": "^11.4.0",
    "glob": "^13.0.6",
    "handlebars": "^4.7.9",
    "ioredis": "6.0.0",
    "jsonwebtoken": "^9.0.3",
    "module-alias": "^2.3.4",
    "moment-timezone": "^0.6.3",
    "mongoose": "^9.9.5",
    "nodemailer": "^10.0.2",
    "nodemailer-express-handlebars": "^7.0.0",
    "p-limit": "^7.3.2",
    "qs": "^6.16.0",
    "winston": "^3.19.0",
    "winston-daily-rotate-file": "^5.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/qs": "^6.15.1"
  },
  "_moduleAliases": {
    "@services": "build/services",
    "@models": "build/models",
    "@helpers": "build/helpers"
  }
}